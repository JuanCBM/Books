const User = require('./user');
const Book = require('./book');
const Comment = require('./comment');

module.exports = {Book:Book,User:User,Comment:Comment}

console.log('Executing Model: index.js ...');
